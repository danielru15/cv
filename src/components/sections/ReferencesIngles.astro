---
import Section from "../Section.astro";
import { references } from "@cv"
---

<Section title="References">
    <ul>
        {
            references.map(({name,occupation,phone}) => (
                <li>
                    <article>
                        <h3>{name}</h3>  
                        <p>Professional title or position: {occupation}</p>
                        <p><a
                        href={`tel:${phone}`}
                        title={`Call ${name} at ${phone}`}
                        target="_blank"
                        rel="noopener noreferrer"
                      >
                       Phone: {phone}
                      </a></p>
                    </article>
                    
                  
                </li>
            ))
        }
    </ul>
</Section>

<style>
    ul {
      display: flex;
      flex-direction: column;
      gap: 32px;
    }
  
    article h3 {
      font-weight: 500;
      color: #111;
    }
    article a {
        color: #727272;
      }
    
      article a:hover {
        text-decoration: underline;
      }
  
     
  </style>
